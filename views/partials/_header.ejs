<header>
  <nav class="navbar navbar-expand-md navbar-dark bg-success">
    <a class="navbar-brand" href="/urls">TinyApp</a>
    <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarNavAltMarkup"
      aria-controls="navbarNavAltMarkup" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarNavAltMarkup">
      <div class="navbar-nav">
        <a class="nav-item nav-link" href="/urls">My URLs</a>
        <a class="nav-item nav-link" href="/urls/new">Create New URL</a>
      </div>
    </div>
    <div>
      <% if (!user) { %>
        <a href="/register"><button type="submit" class="btn btn-primary btn-sm">Register</button></a>
        <a href="/login"><button type="submit" class="btn btn-primary btn-sm">Login</button></a>
        <% } else { %>
          <form method="POST" action="/logout">
            <label for="logout">Logged in as: <%= user.email %></label>
            <button type="submit" id="logout" class="btn btn-primary btn-sm">Logout</button>
          </form>
          <% } %>
    </div>
  </nav>
</header>